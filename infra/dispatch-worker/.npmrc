; This subproject ships to Cloudflare, not the Cisco internal artifactory.
; Pin it to the public npm registry so `npm install` works regardless of
; the user's global ~/.npmrc (which may point at engci-maven).
registry=https://registry.npmjs.org/
